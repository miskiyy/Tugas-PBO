import java.awt.Image;

public class Bird extends Block {
    public Bird(int x, int y, int width, int height, Image img) {
        super(x, y, width, height, img); // Memanggil konstruktor superclass Block
    }
}